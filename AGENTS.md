# قواعد صيانة قالب تطبيق المعلم

## CONTENT UPDATE ONLY

عند طلب تحديث محتوى درس موجود:

1. حدّد الدرس والقسم بالمعرّف الدقيق.
2. عدّل أقل عدد ممكن من ملفات البيانات، وغالبًا `src/data/lessons/<lesson-id>.json` فقط.
3. حافظ على جميع المحتويات الحالية ما لم يُطلب حذفها صراحة.
4. لا تغيّر الهوية أو التنسيق أو التنقل أو Components أو IDs أو `schemaVersion` أو Dependencies إلا بطلب صريح.
5. إذا تطابقت الأسماء بين أكثر من درس ولم يتضح المقصود، اطلب معرّف الدرس أو مساره بدل التخمين.
6. شغّل `npm run check` ثم اختبر الصفحة المتأثرة، وراجع الفرق للتأكد من حصره في المطلوب.

أي تغيير يحتاج migration أو Component أو Dependency أو API ليس Content update. إضافة وظيفة جديدة لا تُنفذ ضمن CONTENT UPDATE ONLY بصورة ضمنية.

مثال:

> أضف الرابط التالي بعنوانه إلى activities في lesson-02، مع الحفاظ على جميع المحتويات الحالية وعدم تغيير أي ملف آخر إلا ما يلزم لهذا التحديث.
